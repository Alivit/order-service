spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  grpc:
    client:
      channels:
        userServiceGrpc:
          address: 'static://localhost:9091'
          secure: false
